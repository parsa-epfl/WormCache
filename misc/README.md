This folder contains random files that are not directly related to the project:
- `cache.c`: The modified version of the cache simulation plugin from QEMU, used for testing the basic functionality of the WormCache.
- `flexus_configuartion.json`: An example json file to describe the Flexus configuration format used by the checkpoint_conversion. 
- `*.jsonc`: The checkpoint format accepted by the Flexus.